<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Registrace – SketchWave</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    .auth-container {
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--sw-bg);
    }
    .auth-card {
      width: 100%;
      max-width: 450px;
      background: var(--sw-surface);
      border-radius: 16px;
      box-shadow: 0 4px 24px rgba(0,0,0,0.08);
      padding: 2.5rem;
    }
    .auth-logo {
      text-align: center;
      margin-bottom: 2rem;
    }
    .auth-logo h1 {
      font-size: 2rem;
      font-weight: 700;
      color: var(--sw-text);
      margin-bottom: 0.5rem;
    }
    .auth-logo p {
      color: var(--sw-muted);
      font-size: 0.95rem;
    }
    .form-control:focus {
      border-color: var(--sw-muted);
      box-shadow: 0 0 0 0.2rem rgba(108, 117, 125, 0.25);
    }
    .btn-primary {
      background: var(--sw-text);
      border: none;
      padding: 0.75rem;
      font-weight: 500;
      transition: all 0.2s;
    }
    .btn-primary:hover {
      background: var(--sw-muted);
      transform: translateY(-1px);
    }
    .divider {
      display: flex;
      align-items: center;
      text-align: center;
      margin: 1.5rem 0;
      color: var(--sw-muted);
      font-size: 0.85rem;
    }
    .divider::before,
    .divider::after {
      content: '';
      flex: 1;
      border-bottom: 1px solid var(--sw-border);
    }
    .divider span {
      padding: 0 1rem;
    }
    .btn-google {
      width: 100%;
      padding: 0.75rem;
      border: 1px solid var(--sw-border);
      background: white;
      color: var(--sw-text);
      font-weight: 500;
      transition: all 0.2s;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 0.5rem;
    }
    .btn-google:hover {
      background: var(--sw-bg);
      border-color: var(--sw-muted);
    }
    .google-icon {
      width: 20px;
      height: 20px;
    }
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="auth-card">
      <div class="auth-logo">
        <h1>SketchWave</h1>
        <p>Vytvořte si účet a začněte kreslit</p>
      </div>

      <form id="registerForm" action="/register" method="POST">
        <div class="mb-3">
          <label for="username" class="form-label">Uživatelské jméno</label>
          <input type="text" class="form-control" id="username" name="username" required placeholder="Zadejte uživatelské jméno">
        </div>

        <div class="mb-3">
          <label for="email" class="form-label">Email</label>
          <input type="email" class="form-control" id="email" name="email" required placeholder="vas@email.cz">
        </div>

        <div class="mb-3">
          <label for="password" class="form-label">Heslo</label>
          <input type="password" class="form-control" id="password" name="password" required placeholder="Minimálně 8 znaků">
          <small class="text-muted">Použijte alespoň 8 znaků včetně čísla a speciálního znaku</small>
        </div>

        <div class="mb-3">
          <label for="passwordConfirm" class="form-label">Potvrzení hesla</label>
          <input type="password" class="form-control" id="passwordConfirm" name="passwordConfirm" required placeholder="Zopakujte heslo">
        </div>

        <div class="mb-3 form-check">
          <input type="checkbox" class="form-check-input" id="terms" required>
          <label class="form-check-label" for="terms">
            Souhlasím s <a href="/terms" class="text-decoration-none">podmínkami použití</a>
          </label>
        </div>

        <button type="submit" class="btn btn-primary w-100 mb-3">
          <i class="bi bi-person-plus me-2"></i>Vytvořit účet
        </button>
      </form>

      <div class="divider">
        <span>nebo</span>
      </div>

      <a href="/auth/google" class="btn btn-google text-decoration-none">
        <svg class="google-icon" viewBox="0 0 24 24">
          <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
          <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
          <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
          <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
        </svg>
        Pokračovat s Google
      </a>

      <div class="text-center mt-4">
        <p class="text-muted mb-0">Již máte účet? <a href="/login" class="text-decoration-none fw-semibold">Přihlásit se</a></p>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" crossorigin="anonymous"></script>
  <script>
    // Client-side validace
    document.getElementById('registerForm').addEventListener('submit', function(e) {
      const password = document.getElementById('password').value;
      const passwordConfirm = document.getElementById('passwordConfirm').value;
      
      if (password !== passwordConfirm) {
        e.preventDefault();
        alert('Hesla se neshodují!');
        return false;
      }
      
      if (password.length < 8) {
        e.preventDefault();
        alert('Heslo musí mít alespoň 8 znaků!');
        return false;
      }
    });
  </script>
</body>
</html>
